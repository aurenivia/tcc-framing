# Build estatico para GitHub Pages (npm run build:pages).
# VITE_LIVE=0 -> esconde o painel de analise ao vivo e nao carrega o proxy /api.
VITE_LIVE=0
# Base relativa: funciona em user.github.io/<repo>/ sem saber o nome do repo.
VITE_BASE=./
